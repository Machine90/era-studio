
import * as React from 'react';
import { Accordion, Icon, Segment } from 'semantic-ui-react';

export default class AddonInventory extends React.Component {

    public render(): React.ReactNode {
        return (
            <Segment inverted>
            </Segment>
        );
    }
}